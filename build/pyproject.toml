[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bureaubuild"
description = "Build and register bureau container images"
license = "MIT"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
  "ansible-runner~=2.2.1",
  "pulumi~=3.40.2",
  "pulumi-azure-native~=1.80.0"
]

[project.optional-dependencies]
dev = [
  "flake8~=5.0.4",
  "isort~=5.10.1",
  "mypy~=0.982"
]

[tool]
